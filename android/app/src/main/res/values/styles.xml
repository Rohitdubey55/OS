<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>

        <!-- Android 15 forces edge-to-edge (status bar transparent, content draws
             under it) for apps targeting SDK 35+. This opt-out flag restores the
             pre-15 behavior where the system pads content below the status bar,
             so we don't need fragile safe-area-inset workarounds in CSS. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
        <item name="android:statusBarColor">@color/colorSplashBackground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <!--
        Cold-launch splash theme. Uses the Android 12+ SplashScreen API where
        available, and falls back to a dark windowBackground on older devices
        so we never see a white flash before the WebView paints.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 12+ proper splash -->
        <item name="windowSplashScreenBackground">@color/colorSplashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>

        <!-- Legacy fallback (Android 11 and below) -->
        <item name="android:windowBackground">@color/colorSplashBackground</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>